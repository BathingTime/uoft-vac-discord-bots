[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "frodobot-common"
version = "0.1.0"
description = "Common utilities for VAC Discord bots"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
